{
  "name": "impious-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:staging": "vite build --mode staging",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "verify:bundle": "npm run build -- --mode production && git diff --exit-code public"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "@types/three": "^0.161.0",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  }
}
