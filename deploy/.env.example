#
# Copy this file to deploy/.env and fill in real values before running docker compose.
# The defaults keep paths relative to the deploy/ directory so NixHQ can clone
# `/opt/impious/deploy` and run compose without editing the manifests.
#

# Email address Caddy will register with ACME for impious.io certificates.
CADDY_ADMIN_EMAIL=ops@example.com

# Location of the built static bundle to serve from /srv/site inside the caddy container.
# Leave the default to use the committed site/public artifacts.
SITE_BUNDLE_PATH=../site/public

# Host path that contains codex.imperiumsolis.com artifacts. This can be a bind-mounted
# directory that another repo populates (e.g., /opt/impious/codex-dist).
CODEX_PAYLOAD_HOST_PATH=../codex-payload

# Separate path for dev/staging codex payloads. Override if you want to point at a different sandbox.
CODEX_PAYLOAD_DEV_PATH=../codex-payload-dev

# Optional override for the future game API image. The service stays disabled unless you pass --profile game.
GAME_API_IMAGE=ghcr.io/intunewithnature/game-api:main
