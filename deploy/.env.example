# Copy this file to deploy/.env and edit it before running docker compose.
# Production operators MUST replace the placeholder email with a deliverable ACME contact.
CADDY_ADMIN_EMAIL=admin@impious.test

# Optional: override the codex payload directory that production Caddy serves.
# Leave commented to use ../codex-payload relative to deploy/.
# CODEX_PAYLOAD_HOST_PATH=../codex-payload

# Optional: override the codex payload directory used by the dev compose override.
# Leave commented to use ../codex-payload-dev relative to deploy/.
# CODEX_PAYLOAD_DEV_PATH=../codex-payload-dev

# Optional: override the static site bundle bound to /srv/site in the caddy container.
# Leave commented to use ../site/public relative to deploy/.
# SITE_BUNDLE_PATH=../site/public

# Optional: point the future game-api service at a different container image.
# Leave commented unless you plan to run --profile game.
# GAME_API_IMAGE=ghcr.io/intunewithnature/game-api:main

# Optional: flip to "1" to let Caddy proxy game.imperiumsolis.com to game-api:3000.
# Production defaults to the safe stub (GAME_API_ENABLED=0); the dev override defaults to 1.
# GAME_API_ENABLED=0
