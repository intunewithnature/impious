# Impious lore site

Static marketing page for `impious.io`, built with Vite, React, and TypeScript.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The compiled assets land in `dist/` and are served by Nginx in production.
